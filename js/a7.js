function changeImage(element, file) {
    // document.getElementById(element).src = window.location.protocol + "//" + window.location.hostname + "/" + file;
    // console.log(document.getElementById(element).src)
    // console.log(window.location.protocol + "//" + window.location.hostname + "/" + file);
    document.getElementById(element).src = file;
}